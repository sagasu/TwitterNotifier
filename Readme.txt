This is a basic App to notify you each time when a new tweet arrives.

Because Twitter API allowed you to query it only few times per hour. In this approach, modifications are checked against web site directly.
